# Entornos virtuales (cada integrante crea el suyo, NUNCA se sube)
venv/
.venv/
env/

# Python
__pycache__/
*.py[cod]

# Base de datos: se genera sola, nunca se versiona
*.db
*.sqlite3
*.db-wal
*.db-shm

# Secretos: el SECRET_KEY de produccion vive en variables de entorno
.env
*.env

# Bancos de prompts personales: SI se versionan (docs/VUP_V2/prompts_*/).
# Son la evidencia de planificacion asistida por IA que pide la rubrica y
# ademas asi tienen copia de seguridad. La evidencia formal sigue en docs/prompts/.
_scratch/

# Editores / SO
.vscode/
.idea/
.DS_Store
Thumbs.db

# Git Bash en Windows crea un archivo llamado NUL con "2>NUL" en vez de descartar.
NUL
